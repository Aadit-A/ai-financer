// This component is now integrated into the Dashboard wireframe
// No separate implementation needed for wireframe phase

const IncomeExpenseForm = () => {
  return (
    <div style={{ 
      padding: '1rem', 
      border: '1px dashed #ccc', 
      borderRadius: '4px',
      textAlign: 'center',
      color: '#666',
      fontFamily: 'monospace'
    }}>
      [This form is now part of the Dashboard wireframe]
    </div>
  );
};

export default IncomeExpenseForm;
